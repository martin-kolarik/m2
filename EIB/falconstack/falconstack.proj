<Project
    DefaultTargets = "Build"
    xmlns="http://schemas.microsoft.com/developer/msbuild/2003" >

    <!-- 1 -->
    <PropertyGroup>
        <FileDescription>EIB/KNX communication stack</FileDescription>
        <FileVersionMajor>2</FileVersionMajor>
        <FileVersionMinor>0</FileVersionMinor>
    </PropertyGroup>

    <!-- 2 INCLUDE DEFINITION -->
    <Import Project="..\..\Build\common.rootdir.proj"/>
    <Import Project="$(RootBuildDir)\common.definition.proj"/>

    <!-- 3 COMPOMENT DEFINITION -->
    <ItemGroup>
        <Components Include="modula2\modula2;api\clib;API\win32"/>
        <Components Include="API\COM;modula2\m2rtl;library\ADT;library\COM;eib\eibstack"/>
        <Modules Include="falcon;falconstack"/>
    </ItemGroup>

    <!-- 5 LAST INCLUDE COMPILATION CODE ITSELF -->
    <Import Project="$(RootBuildDir)\common.build.proj"/>

</Project>

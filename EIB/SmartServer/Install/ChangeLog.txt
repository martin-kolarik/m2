Version 1.2.1400/R 17.11.2010
- CHG: #235, a new function ${msg(...)} was introduced to pt.xml processing. The function
       allows dynamic creation of message identifiers. 
- NEW: #236, strings stored for SDAP clients are not limited to KNX length (14 bytes)
       anymore.

Version 1.2.1312/R 3.9.2010
- BUG: #227, model name of length 1 character was not recognized in pt.xml files
       properly, which could cause various problems with pt.xml rendering (infinite
       looping in pt:for etc.).
- BUG: #229, index.pt.xml does not use pt:access and thus it is not able being protected
       from unauthorized access
- BUG: #230, pt.xml without target root element was not rendered, text files (js, txt) were
       impossible to render.
- BUG: #232, if session expired during userLogin.pt.xml page displayed, SmartServer
       redirected web to admin login page. Now it redirects to index page.
- NEW: #228, logout page was extended with "nextpage=<page>" parameter to redirect web
       after logout. Default behavior was changed from redirecting to login page to
       redirecting to index page.
- NEW: #231, session validity time is now configurable using "session_validity" parameter
       of "[server]" section.

Version 1.2.1291/R 30.7.2010
- BUG: #226, functions (get, getwix) were not registered for admin pages (index etc.)

Version 1.2.1285/R 26.7.2010
- BUG: #214, heavily communicating SDAP/XMLSocket clients could block server
       after client disconnect in infinite wait. Next, the server was not able
       to reconnect KNX connection if it had been lost.
- BUG: #215, race condition in web server caused server crash. The race condition
       occured if single HTTP client asked particular page more times during a short
       time.
- BUG: #223, users defined through admin web interface were lost after server restart.
- BUG: #225, javascript function CorrectNumber worked improperly for value 0 send
       as being not string.
- CHG: #224, if server's function (Get, Set) failed operating KNX, whole call was
       taken as failed and function call remained visible in browser URI. Now
       the function is meant as failed only if it is missing or it has bad number
       of the parameters.

Version 1.2.1227/R 22.5.2010
- BUG: #208, "System.Licensing.SerialNumber" contained invalid XML characters, which
       damaged XMLSocket transfer of the item
- BUG: #210, configuration file examples contained bad names of administration roles       
- CHG: #64, predefined users and roles cannot be renamed either deleted
- CHG: #207, when data is sent through HTTP, the stream can disconnect prematurely.
       Now the state is considered being fully correct.
- CHG: #211, XMLSocket interface now returns all cached data even if KNX is not
       connected. Now XMLSocket behaves the same way as SDAP.
- NEW: #205, SOFT PLC and iWebKit examples description was added to documentation

Version 1.2.1218/R RC2 19.5.2010
- BUG: #203, in documentation, preformatted texts contained bad accented characters 
- BUG: #208, "getWix" function was renamed to "getwix", as it was intended
- CHG: #199, in cache_mode only, web interface does not display error message after
       direct write, it displays "Write succeeded"
- CHG: #202, "getwix" function and "Wix" program was more described in the documentation,
       new examples for "Wix" setup and "getwix" function were added to installation
- CHG: #206, possibility to log to used pages with name/password was reduced to log in
       using password (key or code) only              
- NEW: #200, "Changes.txt" file link was added to StartMenu
- NEW: #200, "default.cfg" file edit link was added to StartMenu
- NEW: #204, new example "11 intervals.pt.xml" with relational operator and text
       coloring added
- NEW: #205, "RCWare" (soft PLC) description, libraries and link to the vendor including
       documentation were added
  
Version 1.2.1201/R RC1 2.5.2010
- BUG: #85, if get of two variables in pt.xml was written without space (like in
       ${get(3/0/0)}${get(3/0/1)}) the second get was not rendered
- BUG: #150, sometimes "Uptime" displayed in status page showed "60" minutes instead
       of correct "0" minutes
- BUG: #157, line number of "address" parameter of "interface" section was always
       badly read as 0
- BUG: #158, "discover" tool occasionaly crashed if more than single interface was
       present in the network
- BUG: #162, comment lines present in configuration file could cause ignoring of parameters
       after comment line or reporting of strange errors
- BUG: #163, if "?set(addr,)" appended to URI was used, SmartServer could crash. If set
       was filled with both parameters the crash never occured.
- BUG: #164, example pages and some of administration web server pages had bad encoding
- BUG: #174, in some configuration files counting of lines was improper, line number
       was bigger than expected
- BUG: #175, DLLs had bad ProductVersion
- BUG: #177, if client requested language different from Czech or English "?unknown text"
       texts were returned. Now English is used as fallback.
- BUG: #178, "set" command did not accept spaces in value argument
- BUG: #189, "set" command did not accept decimal numbers neither with decimal dot neither
       with decimal comma
- BUG: #190, examples sending decimal value sent values with decimal comma instead of
       decimal dot
- BUG: #195, if input pt.xml file contains two or more child elements inside <pt:template>
       element, the output badly contained more XML declarations. This was fixed, but
       the primary cause, presence of two or more XML roots in output, cannot be repaired with
       SmartServer, it is a responsibility of pt.xml file author.
- CHG: #152, name of Win32 service was changed to EIB/KNX SmartServer
- CHG: #165, pt.xml now can contain elements with namespace prefixes. These are correctly
       rendered to output.
- CHG: #170, C runtime library was dimnished
- CHG: #182, times displayed in logs are in local time
- CHG: #183, Program menu now uses NeNo folder as a root
- NEW: #64, ability to create and use more login names and permissions to different pages
       was added
- NEW: #151, new configuration parameter "name" in section "project" was added,
       the parameter value is displayed in status page
- NEW: #153, new examples of user pt.xml pages were added
- NEW: #166, pt.xml now supports "pt.variable" tag
- NEW: #169, minidumps are now written in unexpected program states
- NEW: #172, new pt.xml function "getwix" was added to support Papouch Wix program
- NEW: #173, pt.xml now supports relational operators
- NEW: #193, SDAP now offers self licence number in "System.Licensing.SerialNumber" object

Version 1.1.1075/R RTM 14.12.2009
- BUG: #161, server wrote negative EIS 5 numbers as malformed positive numbers

Version 1.1.1040/R RTM 1.11.2009
- BUG: #147, bad "tunneling-ACK" was renamed to "tunneling-NAT" in
       default.cfg commentary
- CHG: #144, text in installation was changed to SmartServer
- NEW: #146, Virtual Object Server.pr3 file (project file for ETS3) was added
       to the installation, the project contains virtual device allowing
       binding of group addresses used in SmartServer.
- NEW: #148, new example showing usage of "reader" behavior of objects
       named "13 reader.cfg" was added
- NEW: #149, SmartVision logo was added to the installation

Version 1.1.1021/R RC1 25.10.2009
- BUG: #127, SDAP client/server could internally delay notification of received
       data, so messages could be either lost or delayed for very long time.
- BUG: #129, packet counts displayed on status page were not reset, they
       contained number of all packets received from server start.
- BUG: #134, SDAP server crashed, when pending unsent data were sent after
       client had disconnected.
- BUG: #138, for each received packet, server lost 32 bytes of memory. This
       memory leak led to crash after memory was consumed.
- BUG: #139, -Q parameters of activate tool did not do anything.
- CHG: #130, in documentation, [debug] section name was badly used instead of
       correct [log]
- CHG: #135, description text of "get" in direct io page was changed to be clear
       that value is always returned from the server cache, not from KNX itself.
- CHG: #142, name of installation and documentation was changed to SmartServer.
- NEW: #133, datalog now contains information about originator of set operation.
- NEW: #140, new parameter "cache_only" of "device" section was added. When the
       parameter is true, the server does not connect to KNX it does not report
       errors in SDAP communication. SDAP ans XMLSocket client can use the server
       as cache for data interchanging.
- NEW: #143, new parameter "readers_period" of "behaviours" section was added. When
       the parameter is set (if it has nonzero value), all objects with "reader"
       type are periodically read from KNX. The new "reader" type of objects was
       introduced to mark these explicitely read objects.

Version 1.0.972/R RTM 19.9.2009
- BUG: #121, administration web interface was missing version number.
- BUG: #123, if server's domain name contained (as substring) name of some page
       (e.g. control), redirection to login page after cookie expiration failed
       with damaged URL.
- BUG: #126, installations were missing ProductGUID, thus new installations were
       not able to reinstall/detect previous ones.
- CHG: #124, "liccli.exe" tool replaced with "activate.exe"
- CHG: #125, better description of data object commentary added to documentation.
- NEW: #99, new example of Pronto TSU9400 application added.

Version 1.0.922/R rc1 23.8.2009
- BUG: #100, reconnection to KNX could stop if there was some timeouts during
       starting and stopping listening. The cause of timeouts is not clear yet,
       but reconnection does not stop either in that case. 
- BUG: #97, thermostate buttons of ProtoEdit templates referenced not existing
       communication object. 
- BUG: #115, line numbers reported during configuration reading were bad -- lines
       with commentaries were not counted.
- CHG: #107, documentation extended with log messages examples of some common
       communication events.
- CHG: #109, documentation of ProntoScript extended. 
- TST: #106, initial reading revised, there was suspicion that initial reading blocks
       other operations. The suspicion was cleared.
- NEW: #108, English documentation was added to English installation.
- NEW: #105, XMLSocket interface is now able connect and disconnect KNX interface
       using <connect/> and <disconnect/> commands.
- NEW: #102, [log] and [networklog] sections was supplied with filtering
       (filter parameter of log configuration sections), which allows removing
       messages by their sources. 
- NEW: #101, added logging of server's service start and stop.
- NEW: #99, added TSU9600 template for ProntoEdit Professional 2.
- NEW: #103, new logs [httplog] and [networklog] added; first one logs http
       accesses in common log format, second one logs connectioning of SDAP and
       XMLSocket clients.
- NEW: #104, access to web interface can be restricted by rules written in
       [http_access_log] configuration section.
- NEw: #113, when connected to KNX, status web page shows IP address of KNX interface.
- NEW: #114, data objects defined in configuration can be supplied with commentary,
       which is then displayed in data log. 
- NEW: #120, SDAP and XMLSocket clients are now able to read/let advise "Control.Connected"
       data object, which reports state of KNX connection.

Version 1.0.850/R rc1 26.4.2009
- BUG: #94, server crashed unexpectedly, connecting and disconnecting
       XMLSocket client should speed up the crash. Server sent data to XMLSocket
       client after its disconnecting, it used deallocated memory for it.
- BUG: #93, when stopping service, the crash could occur in some cases. Connection
       to KNX was deallocated before last KNX frame was processed.
- NEW: #91, there is english installer now, installers differ in name (*_en.msi, ...)
- NEW: #92, a tool for starting and stopping services, "svcinst", was added to
       installation. Separate example for thermostate page was added too.

Version 1.0.841/R rc1 23.4.2009
- BUG: #89, server crashed after activation and restart

Version 1.0.835/R rc1 13.4.2009
- BUG: #88, named licence was badly displayed in Web interface

Version 1.0.833/R rc1 13.4.2009
- BUG: #72, some Windows XP Embedded systems are missing xmllite.dll. Now the file
       is installed in the case.
- BUG: #73, history of values communicated in last day/hour was not cleared for
       passed hours. 
- BUG: #74, a tool for licence activation was not installed.
- BUG: #71, direct write from web interface did not work if another client session
       was directed to server from the same browser.
- CHG: #30, complete description of web interface was added to documentation.
- CHG: #78, web page after "?set" function usage is redirected to self to not
       to leave "?set" being a part of browser's URL. 
- CHG: #38, CFG examples was supplied with new parameters and sections ([web], [users]).
- CHG: #62, EIBNet/IP connection status codes were added to documentation.
- CHG: #76, default.cfg installed now contains all possibilities shortly described
       (and commented). 
- NEW: #83, examples of user web pages added.
- NEW: #15, system and data logs are now downloadable using link on the page.
- NEW: #61, new [log] and [datalog] sections parameter named "cached" configures
       count of historical records which server remembers.
- NEW: #57, copyright line of web interface now contains version number. Also,
       licence number and owner is displayed. 
- NEW: #59, new object type of [objects] and [block] sections added,
       "logged_on_change_objects" and "logged_on_change_object" limit datalog entries
       to object's value change only.

Version 1.0.799/R beta 15.3.2009
- BUG: #51, space in form item was badly parsed in server, e.g. configuration was
       unable to load if path contained spaces ("C:\Program Files...").
- BUG: #54, possible bad view file (pt.xml) could cause infinite loop during
       sending error response to client.
- BUG: #56, if root of web (SmartServer) was accessed directly, without "/login" or so,
       server crashed.
- BUG: #24, list with configuration errors overlapped lower bar.
- BUG: #37, default.cfg was not installed.
- BUG: #55, a message with redirect ("Page was moved") was displayed and the client did
       not redirected self to new page. Bad content-length header was generated.
- BUG: #60, Google Chrome requires login twice.
- BUG: #63, IE displayed graphics on pages slowly, partially solved making transparent
       images bigger.
- BUG: #18, Login form button was aligned with editboxes.
- BUG: #23, Design of configuration form changed, edibox is long, button is on left.
- BUG: #49, System log is unable to configure from configuration file.
- BUG: #47, Server returned bad HTTP status code (500) if page was not found (404).
- CHG: #58, Data log displays events from now to history.
- CHG: #68, Product icon changed.
- NEW: #34, "Logout" button was added.
- NEW: #35, User roles "admin" and "user" has been added, now with default passwords
       "admin" and "user".
- NEW: #36, Access to server from HTTP clients is thread safe now.

Version 1.0.743/R beta 5.3.2009
- BUG: #52, direct set value using web was reported as success even if server was
       not connected to KNX in the time.
- BUG: #48, data and config logger configurations logged only critical error and
       debug error messages.
- BUG: #46, server crashed if web was accessed without "SmartServer" in URL.
- BUG: #28, message file for web was not loaded in some cases.
- BUG: Page.pt.html file would not be installed.
- BUG: #19, server did not offer English/Czech language if a client requested
       "cs" or "en" language (not "cs-CZ" or "en-US").

Version 1.0.733/R beta 3.3.2009
- BUG: Installer did not install SCMsg.dll.
- NEW: Installer extended with Web files and default.cfg.

Version 1.0.732/R beta 1.3.2009
- BUG: A big number of responses to client in single chunk (> approx. 250) together
       with client connection requests caused long delays in processing of requests.
       Result appeared like server hung up, it was not possible to stop service
       in given timeout, data did not flow, etc.
- CHG: Majority of imported OS libraries is now loaded as delayed.
- CHG: SDAP client was extended with ADVISE, preparation for SDAPClient.
- NEW: Alpha version of web interface added, functions are still incomplete.

Version 1.0.646/R beta 5.11.2008
- CHG: Versioning switched from SVN to HG
- BUG: Closed client connection notified server with two notifications. There was
       race condition between these notifications, the second one could access NIL
       pointer.
- BUG: Client which connects/sends/disconnects fast could cause assertion in server.
       Server started reading asynchronously, which could be done after disconnecting,
       out of order. Now data are received in order. The bug could also lead to
       ignoring requests from such fast clients.

Version 1.0.1802 beta 1.10.2008
- BUG: Eib User layer did not report self advise capabilities to XMLSocket adviser
       correctly. This could cause ASSERT after service run.

Version 1.0.1770 beta 6.9.2008
- BUG: CheckCfg.exe did not display error messages
- CHG: XMLSocket <item> tag renamed to <notify> without changing the meaning
- NEW: XMLSocket <ask> tag added, client can use it to ask value from server,
       server responses asynchronously
- NEW: Server is now able to convert time and date values from/to strings using
       format mask. There are new parameters time_as_string, date_as_string,
       time_format and date_format in configuration file.    

Version 1.0.1758 beta 1.9.2008
- BUG: server did not finish XMLSocket data with trailing zero, data were
       not received with Flash

Version 1.0.1752 beta 12.8.2008
- NEW: added XMLSocket interface, configuration examples and first version
       of documentation

Version 1.0.1707 beta 14.7.2008
- BUG: receiving of many small packets could cause double processing
       ot received packets
- CHG: corrected version numbers of bundled components

Version 1.0.1702 beta 13.7.2008
- BUG: many bugs in SDAP processing (locking under heavy load, problems with
       connection close during accept)
- BUG: EIBNet processing could read packet from different source, if the source
       was present in the net and if process has heavily loaded.
- CHG: development changes in support code

Version 1.0.1134 beta 15.10.2007
- NEW: reading ESF file (export from ETS)
- NEW: initial version of sdap
- NEW: running as service

Version 1.0.850 beta 26.7.2007
- NEW: first alpha version
- NEW: with dynamic CRT

Version 1.0.919 26.7.2007
- INI: created

<Project
    DefaultTargets = "Build"
    xmlns="http://schemas.microsoft.com/developer/msbuild/2003" >

    <!-- 1 -->
    <PropertyGroup>
        <FileDescription>ControlWeb driver interface/helpers</FileDescription>
        <FileVersionMajor>0</FileVersionMajor>
        <FileVersionMinor>1</FileVersionMinor>
    </PropertyGroup>

    <!-- 2 INCLUDE DEFINITION -->
    <Import Project="..\..\Build\common.rootdir.proj"/>
    <Import Project="$(RootBuildDir)\common.definition.proj"/>

    <!-- 3 COMPOMENT DEFINITION -->
    <ItemGroup>
        <Components Include="modula2\modula2;api\clib;API\win32"/>
        <Components Include="modula2\m2rtl;modula2\m2ortl;API\COM;Library\COM;Library\ADT;Library\Messaging;Library\SDIO"/>
        <Modules Include="drv_str;drv_def;drv_wrapper_;drv_wrapper;drv_ax_;drv_ax"/>
    </ItemGroup>

    <!-- 5 LAST INCLUDE COMPILATION CODE ITSELF -->
    <Import Project="$(RootBuildDir)\common.build.proj"/>

</Project>
